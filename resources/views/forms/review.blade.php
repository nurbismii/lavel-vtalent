<x-layouts.form title="Tinjau jawaban"><div class="heading"><div><div class="eyebrow">Satu langkah lagi</div><h1>Periksa sebelum dikirim</h1><p class="muted">Setelah dikirim, perubahan memerlukan pembukaan revisi oleh HR.</p></div></div>
@include('forms.answers', ['fields' => $intake->version->fields, 'answers' => $response->answers, 'documents' => $response->documents->where('selected', true), 'name' => $response->name, 'email' => $response->email])
<form method="post" action="{{ route('forms.submit', $response->reference) }}" class="panel">@csrf<input type="hidden" name="lock_version" value="{{ $response->lock_version }}"><label class="form-check"><input type="checkbox" name="consent" value="1" required> Data dan dokumen sudah benar. Saya menyetujui pengiriman final dan pemrosesan untuk rekrutmen.</label><div class="form-actions"><a class="button" href="{{ route('forms.response', $response->reference) }}">Kembali</a><button class="button primary">Kirim final</button></div></form>
</x-layouts.form>
